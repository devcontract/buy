// install gulp globally

$ npm install gulp-cli -g

// check if gulp is now installed

$ gulp -v

// configure your project in terminal

$ mkdir /path/to/your/project

$ cd /path/to/your/project

$ npm init

//Note for Git users: Node.js installs modules to a node_modules folder.
//You should add this to your .gitignore file to ensure they’re not
// committed to your repository. When deploying the project to another PC,
// you can run npm install to restore them.

// now create structure in your project folder

$ cd /path/to/your/project

$ mkdir -p src/{html,images,js,scss} build/{html,images,js,css}

//install gulp locally

$ npm install gulp --save-dev


// Create a new gulpfile.js configuration file in the root
// of your project folder. Add some basic code to get started:

// Gulp.js configuration
  var
    // modules
    gulp = require('gulp'),

    // development mode?
    devBuild = (process.env.NODE_ENV !== 'production'),

    // folders
    folder = {
      src: 'src/',
      build: 'build/'
    }
  ;
//

//now lets have an example with some gulp packages
//lets install package

$ npm install gulp-htmlclean --save-dev


// this is then referenced at the top of gulpfile.js

var
 // modules
 gulp = require('gulp'),
 htmlclean = require('gulp-htmlclean'),

//We can now create an html task at the end of gulpfile.js:


  // HTML processing
  gulp.task('html', ['images'], function() {
    var
      out = folder.build + 'html/',
      page = gulp.src(folder.src + 'html/**/*');

    // minify production code
    if (!devBuild) {
      page = page.pipe(htmlclean());
    }

    return page.pipe(gulp.dest(out));
  });



// now lets create JAvaScript task

 $ npm install gulp-concat gulp-uglify --save-dev

// Reference them at the top of gulpfile.js:


var
  ...
  concat = require('gulp-concat'),
  uglify = require('gulp-uglify'),


// Then add a new js task:

// JavaScript processing
gulp.task('js', function() {

  var jsbuild = gulp.src(folder.src + 'js/**/*')

    .pipe(concat('main.js'));

  if (!devBuild) {
    jsbuild = jsbuild

      .pipe(uglify());
  }

  return jsbuild.pipe(gulp.dest(folder.build + 'js/'));

});

// now run in the project directory  :

$ gulp js

// make sure that you have JAvaScript files created in src/js folder and html file in src/html respectvly 
